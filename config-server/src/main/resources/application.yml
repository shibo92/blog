server:
  port: 8080

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shibo92/blog.git
          search-paths: config-repo/conf
          force-pull: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://39.107.79.30:8761/eureka/