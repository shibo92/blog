server:
  port: 8082

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shibo92/blog.git
          searchPaths: conf
          force-pull: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/